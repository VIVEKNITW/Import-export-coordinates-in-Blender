# Import-export-coordinates-in-Blender
This add-on helps in reading the coordinate values saved in text file &amp; importing them. Also it can be used to save the coordinates of object in .txt format
